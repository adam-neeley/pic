#!/usr/bin/env python3

from setuptools import setup

setup(
   name='pic',
   version='0.1',
   description='desc',
   author='author',
   author_email='foomail@foo.example',
   packages=['pic'],
   scripts=['./bin/main.py']
)
